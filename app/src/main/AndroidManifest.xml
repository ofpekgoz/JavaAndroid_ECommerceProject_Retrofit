<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omerfpekgoz.eticaretproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.UrunSilActivity"></activity>
        <activity android:name=".activity.UrunGuncelleActivity" />
        <activity android:name=".activity.UrunlerimActivity" />
        <activity android:name=".activity.UrunEkleActivity" />
        <activity android:name=".activity.MusteriMainActivity" />
        <activity android:name=".activity.EsnafMainActivity" />
        <activity android:name=".activity.KayitOlActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>